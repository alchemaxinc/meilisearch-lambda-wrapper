# IDE
.idea
.build
dist/

# OSX
*.DS_Store

# Rust
target/

# Terraform — real tfvars files may contain secrets (e.g. meilisearch_master_key).
# Only the tracked *.example file should ever be committed.
docs/terraform_example/terraform.tfvars
*.tfvars
!*.tfvars.example
.terraform/
.terraform.lock.hcl
